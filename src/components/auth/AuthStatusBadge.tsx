/**
 * @deprecated Do not mount a top header auth pill or duplicate logout control.
 * Logout lives only in FeedSidebar (desktop) and MobileProfileDrawer (mobile).
 */
export default function AuthStatusBadge() {
  return null;
}
