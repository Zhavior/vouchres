# Vitest setup — add to package.json devDependencies

"devDependencies": {
  "vitest": "^2.1.0",
  "@vitest/coverage-v8": "^2.1.0",
  "supabase": "^2.0.0",
  "stripe-mock": "^0.42.0"
}

"scripts": {
  "test": "vitest run",
  "test:watch": "vitest",
  "test:coverage": "vitest run --coverage"
}
