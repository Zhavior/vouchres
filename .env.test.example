# Test-only credentials. Never use the production Supabase project here.
# For a local stack, run `supabase start` and copy its API URL and service role key.
SUPABASE_URL_TEST=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY_TEST=

# Optional: only needed by the Stripe-backed billing suite.
STRIPE_SECRET_KEY=sk_test_replace_with_a_test_key
