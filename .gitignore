node_modules
dist
.env
.env.local
.env.*.local
.env.local.*
.DS_Store
.vite
.vite-temp
.vitest-cache
.vercel

# Local patch backups - keep outside Vite source tree
src/**/*.before*.ts
src/**/*.before*.tsx
src/**/*.backup*.ts
src/**/*.backup*.tsx
_code_backups/

.env
.env.*
!.env.example
.env.sentry-build-plugin
*.zip
__MACOSX/
.DS_Store

# Aurora generated artifacts
aurora/generated/*.json
*.dmg

# Local temp
*.bak
PORT=3000
npm
drafts/
.cache/

# Accidental Finder-style duplicate copies (verified byte-identical).
# This listed only .ts/.tsx/.css, so duplicate .webp, .mjs and .sql copies —
# including a second copy of a Supabase migration — stayed visible to
# `git add -A` and were committed by accident. Match any extension, plus
# extensionless files and duplicated directories.
**/* 2.*
**/* 2
**/* 2/
front-page-lab-visual *.html

# Internal documentation (not for public GitHub)
docs/internal/
.env*
!.env.test.example
